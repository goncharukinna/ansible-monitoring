- name: Install packages on containers
  hosts: containers
  become: yes
  tasks:
    - name: Update apt cache
      ansible.builtin.apt:
        update_cache: yes

    - name: Install required packages
      ansible.builtin.apt:
        name:
          - curl
          - git
          - vim
        state: present